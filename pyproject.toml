[project]
name = "beyond-fals"
version = "0.1.0"
description = ""
authors = [
    {name = "Diego Villanueva Terrazas",email = "diegovt.arbt@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi (>=0.121.1,<0.122.0)",
    "uvicorn (>=0.38.0,<0.39.0)",
    "pytest (>=9.0.0,<10.0.0)",
    "mlflow (>=3.6.0,<4.0.0)",
    "huggingface-hub (>=1.1.2,<2.0.0)",
    "datasets (==3.6.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
